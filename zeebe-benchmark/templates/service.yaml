apiVersion: v1
kind: Service
metadata:
  name: {{ include "zeebe-benchmark.fullname" . }}
  labels:
    {{- include "zeebe-benchmark.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zeebe-benchmark.selectorLabels" . | nindent 4 }}
