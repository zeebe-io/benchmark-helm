apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zeebe-benchmark.fullname" . }}-test-connection"
  labels:
    {{- include "zeebe-benchmark.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zeebe-benchmark.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
